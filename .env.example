# ─────────────────────────────────────────────────────────────
# 環境變數範本 —— 複製為 .env 後填入
#
# 這裡的每一項都是「選填」。全部不設定，應用仍然完整可用：
#   - 儲存自動退回本地 IndexedDB
#   - 個股研究切換到「手動輸入」模式
# 這是刻意的設計：作品集的讀者不該為了跑起來而先去申請兩個服務的金鑰。
#
# 注意：Vite 會把所有 VITE_ 開頭的變數**打包進前端 bundle**，
# 使用者可以在瀏覽器裡看到它們。因此這裡只能放公開金鑰，
# 真正的權限控制必須在服務端（Firestore 安全規則、API 用量限制），
# 絕不能依賴「金鑰藏在前端」這件事。
# ─────────────────────────────────────────────────────────────


# ── Firebase 雲端同步（選填） ──
#
# 不設定 → 使用本地 IndexedDB，資料只存在這台裝置。
# 設定後 → 跨裝置同步；初始化失敗時自動退回 IndexedDB 並在畫面上說明原因。
#
# 取得方式：Firebase Console → 專案設定 → 一般 → 你的應用程式
# 另需啟用：Authentication → Sign-in method → 匿名登入
# Firestore 安全規則見 src/services/firestoreRepository.ts 檔頭
#
# 啟用判斷只看前三項（API_KEY / AUTH_DOMAIN / PROJECT_ID）是否都有值，
# 見 src/services/firebaseConfig.ts。其餘三項仍應一併填寫 —— 缺了它們
# Firebase 初始化會在執行期失敗，屆時應用會退回 IndexedDB 並顯示原因。

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=


# ── Alpha Vantage 股票資料（選填） ──
#
# 不設定 → 個股研究只能用「手動輸入」模式（功能完全一致）。
# 免費金鑰：https://www.alphavantage.co/support/#api-key
#
# 免費方案限制：每分鐘 5 次、每日 25 次。用戶端內建請求佇列與快取
# （報價 30 分鐘、基本面 2 小時）來節省配額，見 services/alphaVantage/client.ts
#
# 已知資料限制：免費 OVERVIEW 端點不提供 DebtToEquityRatio 與 CurrentRatio。
# 缺漏欄位會成為 null 而非 0，該指標退出評分計算（不計零分也不計滿分），
# 並在畫面上標示「未納入評分」。要納入這兩項請改用手動輸入模式。

VITE_ALPHA_VANTAGE_API_KEY=


# ── API 端點（選填） ──
#
# 只有在需要指向測試替身或代理時才設定，正常情況留空即可。

VITE_API_BASE_URL=https://www.alphavantage.co
